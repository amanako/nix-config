{
  den.quirks.desktopEntries = {
    description = ''
      Additional desktop entries to display in launchers and other places.
      Most packages come with desktop applications alongside so this option
      should be used only if there is a valid reason for overriding default
      behaviour on those or if one isn't provided.
    '';
  };
}
