---
name: CI

on:
  pull_request:
  push:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    steps:
      - name: Test build
        uses: ./.github/workflows/build.yml
        secrets: inherit
