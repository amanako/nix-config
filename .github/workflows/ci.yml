name: CI

on:
  pull_request:
  push:

permissions:
  id-token: write
  contents: read

jobs:
  call-build:
    uses: ./.github/workflows/build.yml
    secrets:
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
